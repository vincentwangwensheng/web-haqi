import { ImageShowDirective } from './image-show.directive';

describe('ImageShowDirective', () => {
  it('should create an instance', () => {
    const directive = new ImageShowDirective();
    expect(directive).toBeTruthy();
  });
});
