import { NewDateTransformPipe } from './new-date-transform.pipe';

describe('NewDateTransformPipe', () => {
  it('create an instance', () => {
    const pipe = new NewDateTransformPipe();
    expect(pipe).toBeTruthy();
  });
});
