import { FilterArrayByFieldsPipe } from './filter-array-by-fields.pipe';

describe('FilterArrayByFieldsPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterArrayByFieldsPipe();
    expect(pipe).toBeTruthy();
  });
});
