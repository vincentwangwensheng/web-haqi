import { JoinObjectByFieldPipe } from './join-object-by-field.pipe';

describe('JoinObjectByFieldPipe', () => {
  it('create an instance', () => {
    const pipe = new JoinObjectByFieldPipe();
    expect(pipe).toBeTruthy();
  });
});
