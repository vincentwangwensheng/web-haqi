import { DataThousandsPipe } from './data-thousands.pipe';

describe('DataThousandsPipe', () => {
  it('create an instance', () => {
    const pipe = new DataThousandsPipe();
    expect(pipe).toBeTruthy();
  });
});
