import { ArrayFieldMapPipe } from './array-field-map.pipe';

describe('ArrayFieldMapPipe', () => {
  it('create an instance', () => {
    const pipe = new ArrayFieldMapPipe();
    expect(pipe).toBeTruthy();
  });
});
