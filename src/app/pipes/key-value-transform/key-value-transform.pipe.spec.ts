import { KeyValueTransformPipe } from './key-value-transform.pipe';

describe('KeyValueTransformPipe', () => {
  it('create an instance', () => {
    const pipe = new KeyValueTransformPipe();
    expect(pipe).toBeTruthy();
  });
});
