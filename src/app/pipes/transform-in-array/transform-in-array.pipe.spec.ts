import { TransformInArrayPipe } from './transform-in-array.pipe';

describe('TransformInArrayPipe', () => {
  it('create an instance', () => {
    const pipe = new TransformInArrayPipe();
    expect(pipe).toBeTruthy();
  });
});
