import { GetObjectByFieldsPipe } from './get-object-by-fields.pipe';

describe('GetObjectByFieldsPipe', () => {
  it('create an instance', () => {
    const pipe = new GetObjectByFieldsPipe();
    expect(pipe).toBeTruthy();
  });
});
