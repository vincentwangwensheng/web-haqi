import { AddClassToElementDirective } from './add-class-to-element.directive';

describe('AddClassToElementDirective', () => {
  it('should create an instance', () => {
    const directive = new AddClassToElementDirective();
    expect(directive).toBeTruthy();
  });
});
